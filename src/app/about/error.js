"use client";

export default function ErrorPage({ error, reset }) {
    return (
        <div className="flex justify-center">
            <h1 className="text-center">Error is coming in About Page.</h1>
        </div>
    )
}